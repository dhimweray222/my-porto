# Nuxt TS Starter

A minimal Nuxt 3 + TypeScript starter.

## Scripts

- `dev`: start dev server
- `build`: production build
- `preview`: preview production build
- `lint`: run ESLint
- `format`: run Prettier

## Quick Start

```bash
# from project root
npm install
npm run dev
```

Open http://localhost:3000
